% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_qgsi_desired_gain.R
\name{run_qgsi_desired_gain}
\alias{run_qgsi_desired_gain}
\title{Desired-gain QGSI using trait GEBVs}
\usage{
run_qgsi_desired_gain(
  init_data,
  gebv_data,
  trait_cols,
  id_col = "GenoID",
  dg,
  W_d = NULL,
  quadratic_diag_weights = NULL,
  lower_is_better = NULL,
  center_traits = TRUE,
  scale_traits = TRUE,
  impute_missing = TRUE,
  return_components = TRUE,
  debug = TRUE
)
}
\arguments{
\item{init_data}{A \code{data.frame} or \code{data.table} containing genotype
identifiers and any metadata to preserve in the final output. Typical
columns may include \code{GenoID}, family, population, or other descriptors.
The returned table appends QGSI results to this object.}

\item{gebv_data}{A \code{data.frame} or \code{data.table} containing genotype IDs and
trait GEBVs. It must contain \code{id_col} and all columns listed in
\code{trait_cols}. Each row should represent one genotype and each trait column
should be numeric or coercible to numeric.}

\item{trait_cols}{Character vector giving the trait GEBV column names to use
in the index. These names must be present in \code{gebv_data}. The order of
\code{trait_cols} determines the order used for \code{dg}, \code{W_d}, and any diagonal
weights.}

\item{id_col}{Character string naming the genotype identifier column used to
align \code{init_data} and \code{gebv_data}. Default is \code{"GenoID"}. This column must
be present in both input tables.}

\item{dg}{Named numeric desired-gain vector, with names matching
\code{trait_cols}. This replaces the classical economic-weight vector in the
linear part of the index. For best interpretability, \code{dg} should be on the
same scale as the transformed GEBV matrix used by the function.}

\item{W_d}{Optional full symmetric quadratic desired-gain matrix of dimension
\verb{length(trait_cols) x length(trait_cols)}. This matrix controls the
quadratic and interaction contributions among traits. If \code{NULL}, a diagonal
matrix is created from \code{quadratic_diag_weights}.}

\item{quadratic_diag_weights}{Optional named numeric vector of diagonal
weights used only when \code{W_d = NULL}. Names must match \code{trait_cols}. Larger
values place stronger emphasis on the quadratic contribution of the
corresponding trait.}

\item{lower_is_better}{Optional character vector naming traits for which
smaller values are favorable. These traits are internally multiplied by
\code{-1} so that all traits are aligned in a common favorable direction before
building the index.}

\item{center_traits}{Logical. If \code{TRUE}, the GEBV matrix is centered trait by
trait using the column means before the index is computed. This is useful
when GEBVs are not already centered around zero.}

\item{scale_traits}{Logical. If \code{TRUE}, each trait column is divided by its
standard deviation after optional centering. This is useful when GEBVs are
on different scales or when you want a standardized trait space.}

\item{impute_missing}{Logical. If \code{TRUE}, missing trait values are imputed by
the mean of the corresponding trait column. If \code{FALSE}, the function stops
with an error when missing values are present.}

\item{return_components}{Logical. If \code{TRUE}, the returned ranked table
includes the separate linear and quadratic components in addition to the
final QGSI score. If \code{FALSE}, only the final QGSI score and its rank are
retained.}

\item{debug}{Logical. If \code{TRUE}, the function prints progress and diagnostic
messages during execution. This is useful for tracing data preparation and
matrix construction steps.}
}
\value{
A list containing:
\describe{
\item{dg}{The desired-gain vector used in the linear component.}
\item{W_d}{The quadratic desired-gain matrix used in the quadratic component.}
\item{trait_space_used}{A \code{data.table} containing the transformed GEBV
matrix actually used in the index calculation after optional flipping,
centering, and scaling.}
\item{ranked_geno}{A \code{data.table} containing \code{init_data} augmented with
\code{QGSI_DG}, \code{Rank_QGSI_DG}, and optionally \code{LinearDGPart},
\code{QuadraticDGPart}, \code{Rank_LinearDGPart}, and \code{Rank_QuadraticDGPart},
sorted by decreasing \code{QGSI_DG}.}
\item{component_summary}{A \code{data.table} summarizing the mean, standard
deviation, minimum, and maximum of the computed index components.}
}
}
\description{
Builds a genomic quadratic selection index for all genotypes using the QGSI
structure proposed for nonlinear genomic selection, but replacing the linear
economic-weight vector with a breeder-defined desired-gain vector.

The implemented index is:
\deqn{QGSI\_DG_i = d' \hat{\gamma}_i + \hat{\gamma}_i' W_d \hat{\gamma}_i}

where:
\itemize{
\item \eqn{d} is the desired-gain vector,
\item \eqn{\hat{\gamma}_i} is the vector of trait GEBVs for genotype \eqn{i},
\item \eqn{W_d} is a quadratic desired-gain matrix controlling trait-by-trait
interaction contributions.
}
}
\details{
This function is intended for genomic selection workflows where trait GEBVs
are already available for each genotype.

The function:
\enumerate{
\item aligns \code{init_data} and \code{gebv_data} by \code{id_col};
\item converts all requested trait columns to numeric;
\item optionally imputes missing values by trait means;
\item optionally flips traits listed in \code{lower_is_better} so that all traits
are expressed in a common favorable direction;
\item optionally centers and/or scales the trait GEBVs;
\item computes a linear desired-gain component;
\item computes a quadratic interaction component using \code{W_d};
\item combines both parts into a final desired-gain QGSI score;
\item ranks all genotypes by the resulting index.
}

If traits are already standardized and \code{dg} is already expressed in SD units,
the recommended settings are:
\itemize{
\item \code{center_traits = FALSE}
\item \code{scale_traits = FALSE}
}

because the trait GEBVs and desired gains are already on the same scale.

If \code{W_d} is not supplied, the function builds a diagonal matrix from
\code{quadratic_diag_weights}. In that case, the quadratic contribution captures
only squared trait effects, without off-diagonal trait interactions.
}
\examples{
trait_cols <- c("YLD", "MY", "MI", "BL", "NBL", "VHB")

dg <- c(
  YLD = 1.5,
  MY  = 0.5,
  MI  = 0.5,
  BL  = 1.0,
  NBL = 1.0,
  VHB = 1.0
)

qdiag <- c(
  YLD = 2.0,
  MY  = 1.0,
  MI  = 1.0,
  BL  = 1.5,
  NBL = 1.5,
  VHB = 1.5
)

ext <- system.file("extdata", package = "DGQGSI")
pheno <- data.table::fread(file.path(ext, "example_pheno.csv"))
gebv  <- data.table::fread(file.path(ext, "example_gebv.csv"))

res <- run_qgsi_desired_gain(
  init_data = pheno[, .(GenoID, Family)],
  gebv_data = gebv[, c("GenoID", trait_cols), with = FALSE],
  trait_cols = trait_cols,
  dg = dg,
  quadratic_diag_weights = qdiag,
  lower_is_better = c("BL", "NBL", "VHB"),
  center_traits = FALSE,
  scale_traits = FALSE,
  impute_missing = TRUE,
  return_components = TRUE,
  debug = FALSE
)

head(res$ranked_geno)
res$component_summary

}
\references{
Cerón-Rojas JJ, Montesinos-López OA, Montesinos-López A, et al. (2026).
Nonlinear genomic selection index accelerates multi-trait crop improvement.
\emph{Nature Communications}, 17:1991.
doi:10.1038/s41467-026-69890-3
}
