% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dgsi_qgsi_pipeline.R
\name{run_dgsi_qgsi_pipeline}
\alias{run_dgsi_qgsi_pipeline}
\title{Run a full desired-gain index and desired-gain QGSI pipeline}
\usage{
run_dgsi_qgsi_pipeline(
  mode = c("both", "dg", "qgsi"),
  init_data,
  trait_cols,
  dg,
  id_col = "GenoID",
  cand_data = NULL,
  ref_data = NULL,
  G = NULL,
  select_mode = c("top_n", "trait_thresholds"),
  n_select = 100,
  trait_min_sd = NULL,
  fallback_to_top_n = TRUE,
  n_iter = 1000,
  n_rep = 20,
  sd_scale = 1,
  seed = 42,
  ridge_P = 1e-06,
  ridge_M = 1e-06,
  dg_scale_traits = FALSE,
  gebv_data = NULL,
  W_d = NULL,
  quadratic_diag_weights = NULL,
  qgsi_center_traits = FALSE,
  qgsi_scale_traits = FALSE,
  qgsi_impute_missing = TRUE,
  W_method = c("hybrid", "diag_only", "dg_outer", "corr_weighted"),
  W_base_diag = NULL,
  W_lambda_diag = 1,
  W_lambda_outer = 0.5,
  W_lambda_corr = 1,
  W_corr_power = 1,
  W_offdiag_shrink = 1,
  W_positive_diagonal_only = TRUE,
  W_normalize = c("trace", "max_abs", "none"),
  lower_is_better = NULL,
  merge_outputs = TRUE,
  compare_sort_by = c("DG_rank", "QGSI_rank", "DG", "QGSI", "none"),
  debug = TRUE
)
}
\arguments{
\item{mode}{Character string specifying which workflow to run.
One of \code{"both"}, \code{"dg"}, or \code{"qgsi"}.}

\item{init_data}{A \code{data.frame} or \code{data.table} containing genotype
identifiers and metadata (for example \code{GenoID}, family, population).
This table is preserved and appended with index results.}

\item{trait_cols}{Character vector of trait names used in both DG and
QGSI calculations. These columns must exist in the candidate trait
data (\code{cand_data}) and/or the GEBV data (\code{gebv_data}).}

\item{dg}{Named numeric desired-gain vector whose names correspond to
\code{trait_cols}. This vector defines the breeding objectives and is used
in both DGSI and QGSI calculations.}

\item{id_col}{Character genotype identifier column used to align all
input tables. Default is \code{"GenoID"}.}

\item{cand_data}{Optional \code{data.frame} or \code{data.table} containing
candidate trait values for the desired-gain index workflow.}

\item{ref_data}{Optional reference dataset used for scaling and
covariance estimation in the desired-gain index workflow.}

\item{G}{Optional genetic covariance matrix used in the desired-gain
index optimization. If \code{NULL}, it is estimated from the reference data.}

\item{select_mode}{Selection strategy used in the DG workflow.
One of \code{"top_n"} or \code{"trait_thresholds"}.}

\item{n_select}{Integer specifying the number of selected genotypes
when \code{select_mode = "top_n"}.}

\item{trait_min_sd}{Named numeric vector specifying minimum selection
thresholds when \code{select_mode = "trait_thresholds"}. Values are
expressed in standard deviation units.}

\item{fallback_to_top_n}{Logical. If \code{TRUE}, the function falls back to
top-N selection when threshold selection returns zero genotypes.}

\item{n_iter}{Integer number of optimization iterations used in the
desired-gain index search algorithm.}

\item{n_rep}{Integer number of optimization replicates.}

\item{sd_scale}{Numeric perturbation scale applied to candidate
desired-gain vectors during optimization.}

\item{seed}{Integer random seed controlling reproducibility of the
optimization procedure.}

\item{ridge_P}{Numeric ridge penalty added to the phenotypic correlation
matrix used in the desired-gain index calculation.}

\item{ridge_M}{Numeric ridge penalty added to the inner matrix used
during coefficient computation.}

\item{dg_scale_traits}{Logical. If \code{TRUE}, traits are centered and
scaled before computing the desired-gain index.}

\item{gebv_data}{Optional \code{data.frame} or \code{data.table} containing
genotype-level GEBVs used for QGSI computation.}

\item{W_d}{Optional quadratic desired-gain matrix. If \code{NULL}, the
matrix is automatically generated using
\code{\link[=construct_Wd_from_dg]{construct_Wd_from_dg()}}.}

\item{quadratic_diag_weights}{Optional named numeric vector used to
construct a diagonal \code{W_d} matrix when no full matrix is provided.}

\item{qgsi_center_traits}{Logical. If \code{TRUE}, the GEBV matrix is
centered before computing QGSI.}

\item{qgsi_scale_traits}{Logical. If \code{TRUE}, the GEBV matrix is
scaled before computing QGSI.}

\item{qgsi_impute_missing}{Logical. If \code{TRUE}, missing GEBV values
are imputed by trait means before computing QGSI.}

\item{W_method}{Method used to automatically construct \code{W_d}.
Passed to \code{\link[=construct_Wd_from_dg]{construct_Wd_from_dg()}}. Options include
\code{"hybrid"}, \code{"diag_only"}, \code{"dg_outer"}, and \code{"corr_weighted"}.}

\item{W_base_diag}{Optional baseline diagonal weights for automatic
\code{W_d} construction.}

\item{W_lambda_diag}{Weight applied to the diagonal component when
building \code{W_d}.}

\item{W_lambda_outer}{Weight applied to the desired-gain outer-product
component of \code{W_d}.}

\item{W_lambda_corr}{Weight applied to the correlation-weighted
component of \code{W_d}.}

\item{W_corr_power}{Exponent applied to trait correlations when
constructing the correlation-weighted component.}

\item{W_offdiag_shrink}{Numeric shrinkage factor applied to
off-diagonal elements of \code{W_d}.}

\item{W_positive_diagonal_only}{Logical. If \code{TRUE}, negative diagonal
entries in \code{W_d} are set to zero.}

\item{W_normalize}{Normalization strategy applied to \code{W_d}. One of
\code{"trace"}, \code{"max_abs"}, or \code{"none"}.}

\item{lower_is_better}{Optional character vector listing traits for
which smaller values are favorable. These traits are internally
flipped so that all objectives align in the same direction.}

\item{merge_outputs}{Logical. If \code{TRUE} and \code{mode = "both"}, the
DG and QGSI outputs are merged using
\code{\link[=compare_dg_and_qgsi]{compare_dg_and_qgsi()}}.}

\item{compare_sort_by}{Sorting rule passed to
\code{\link[=compare_dg_and_qgsi]{compare_dg_and_qgsi()}}. Determines how the merged comparison table
is ordered.}

\item{debug}{Logical. If \code{TRUE}, the function prints progress and
diagnostic messages during execution.}
}
\value{
A list that may contain:
\describe{
\item{dg_result}{Output of
\code{\link[=run_desired_gain_index_Joukhadar2024]{run_desired_gain_index_Joukhadar2024()}}.}

\item{W_d_result}{Output of
\code{\link[=construct_Wd_from_dg]{construct_Wd_from_dg()}} when the quadratic matrix is
constructed automatically.}

\item{qgsi_result}{Output of
\code{\link[=run_qgsi_desired_gain]{run_qgsi_desired_gain()}}.}

\item{comparison_result}{Output of
\code{\link[=compare_dg_and_qgsi]{compare_dg_and_qgsi()}} when both workflows are run
and \code{merge_outputs = TRUE}.}
}
}
\description{
High-level wrapper that runs one or both of the desired-gain selection index
optimization workflow and the desired-gain quadratic genomic selection index
(QGSI) workflow, with optional merged comparison output.

This function orchestrates the full DGQGSI pipeline, allowing users to:
\itemize{
\item compute a desired-gain selection index following
Joukhadar et al. (2024),
\item compute a desired-gain quadratic genomic selection index (QGSI)
following Cerón-Rojas et al. (2026),
\item automatically construct the quadratic desired-gain matrix \code{W_d}
when needed,
\item and generate a merged comparison table between the two methods.
}

The function acts as a convenient wrapper that coordinates multiple package
functions, ensuring consistent input alignment and reducing repetitive code
when running complete selection-index analyses.
}
\details{
The pipeline can operate in three modes controlled by \code{mode}:

\describe{
\item{\code{"dg"}}{
Runs only the desired-gain selection index workflow using
\code{\link[=run_desired_gain_index_Joukhadar2024]{run_desired_gain_index_Joukhadar2024()}}.
}

\item{\code{"qgsi"}}{
Runs only the desired-gain QGSI workflow using
\code{\link[=run_qgsi_desired_gain]{run_qgsi_desired_gain()}}. If \code{W_d} is not provided, the matrix is
automatically constructed using \code{\link[=construct_Wd_from_dg]{construct_Wd_from_dg()}}.
}

\item{\code{"both"}}{
Runs both workflows sequentially. If \code{merge_outputs = TRUE},
the results are merged using \code{\link[=compare_dg_and_qgsi]{compare_dg_and_qgsi()}} to produce a
genotype-level comparison table.
}
}

The typical pipeline sequence when \code{mode = "both"} is:

\enumerate{
\item run the desired-gain index optimization;
\item optionally construct a quadratic desired-gain matrix \code{W_d};
\item compute the QGSI scores using GEBVs;
\item optionally merge both outputs into a comparison table.
}

This wrapper is particularly useful in genomic selection pipelines
where breeders want to compare:
\itemize{
\item classical linear desired-gain indices,
\item nonlinear quadratic genomic selection indices,
\item and their resulting genotype rankings.
}
}
\examples{
trait_cols <- c("YLD", "MY", "MI", "BL", "NBL", "VHB")

dg <- c(
  YLD = 1.5,
  MY  = 0.5,
  MI  = 0.5,
  BL  = 1.0,
  NBL = 1.0,
  VHB = 1.0
)

ext <- system.file("extdata", package = "DGQGSI")
pheno <- data.table::fread(file.path(ext, "example_pheno.csv"))
gebv  <- data.table::fread(file.path(ext, "example_gebv.csv"))

res <- run_dgsi_qgsi_pipeline(
  mode = "both",
  init_data = pheno[, .(GenoID, Family)],
  cand_data = pheno[, c("GenoID", trait_cols), with = FALSE],
  gebv_data = gebv[, c("GenoID", trait_cols), with = FALSE],
  trait_cols = trait_cols,
  dg = dg,
  lower_is_better = c("BL", "NBL", "VHB"),
  merge_outputs = TRUE,
  debug = FALSE
)

head(res$comparison_result$comparison_table)

}
\references{
Joukhadar R et al. (2024).
Optimising desired gain indices to maximise selection response.
Frontiers in Plant Science 15:1337388.

Cerón-Rojas JJ et al. (2026).
Nonlinear genomic selection index accelerates multi-trait crop improvement.
Nature Communications 17:1991.
}
\seealso{
\code{\link[=run_desired_gain_index_Joukhadar2024]{run_desired_gain_index_Joukhadar2024()}},
\code{\link[=run_qgsi_desired_gain]{run_qgsi_desired_gain()}},
\code{\link[=construct_Wd_from_dg]{construct_Wd_from_dg()}},
\code{\link[=compare_dg_and_qgsi]{compare_dg_and_qgsi()}}
}
